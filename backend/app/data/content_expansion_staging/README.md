# Content expansion staging

One asynchronous author owns each `chapter_XX_pass_NN.json` file. Files here are
not loaded by the app. They remain staged until validation and human/agent QA
approve explicit publication into curated override files.

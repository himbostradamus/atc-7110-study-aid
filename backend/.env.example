# PostgreSQL connection string for DB-backed API routes and import scripts.
# The codebase currently expects the default SQLAlchemy psycopg2 URL form.
DATABASE_URL=postgresql://atc:atc@localhost:5432/atc_platform
